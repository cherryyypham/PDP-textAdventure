/**
 * Main driver class - instantiates the controller and starts the game
 */
public class Main {
    public static void main(String[] args) {
        Controller controller = new Controller();
        controller.startGame();
    }
}